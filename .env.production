# Produção — piloto na coordenação (bot.vlopinhos.dev).
# Este arquivo é lido pelo Vite quando rodar `npm run build` (que ativa
# o modo production automaticamente). O backend está atrás do Caddy no
# mesmo host, exposto sob /api.
VITE_API_URL=https://bot.vlopinhos.dev/api
