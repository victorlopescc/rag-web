# Produção — piloto na coordenação.
#
# Arquitetura:
#   - Frontend (este app): hospedado no Netlify em https://bot.vlopinhos.dev
#   - Backend (FastAPI): rodando no PC do dev, exposto via Tailscale Funnel
#     com URL fixa em https://victor.taild320f2.ts.net (não muda entre
#     restarts e nem ao trocar de wifi/IP).
#   - WhatsApp (Evolution): envia webhooks pra <tailscale>/webhook
#
# CORS: o backend já libera a origem https://bot.vlopinhos.dev (ver
# backend/main.py). Se trocar o domínio do frontend, atualize lá também.
VITE_API_URL=https://victor.taild320f2.ts.net
